#we ignore the venv
venv

# local run data - created by common.ensure_runtime_dirs().
# Anchored to the root: tests/data holds committed fixtures and must not match.
/data/
/data_agent/
/output/
/tests/data
/reference_run
# rendered by the acceptance tests
/tests/output/

# python bytecode
__pycache__/
*.py[cod]
.pytest_cache